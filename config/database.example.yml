defaults: &defaults
  adapter: postgresql
  pool: 5
  timeout: 5000
  host: localhost
  username: postgres
  password: postgres

development:
  <<: *defaults
  database: ngn_development

test:
  <<: *defaults
  database: ngn_test

production:
  <<: *defaults
  database: ngn_development
